- if user_signed_in?
	= link_to "LogOut", destroy_user_session_path, method: :delete, class: "btn btn-danger"
- else
	= link_to "SignIn", new_user_session_path, class: " btn btn-success "
	= link_to "SignUp", new_user_registration_path, class: "btn btn-primery"