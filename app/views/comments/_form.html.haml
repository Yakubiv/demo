= form_for([@post, @post.comments.build]) do |f| 
	= f.hidden_field :user_id, value: current_user.id 
	= f.hidden_field :post_id, value: @post.id
	= f.text_area :content, placeholder: 'Написати коментар'
	= f.submit 'Готово'